pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
            }
        }

        stage('Deploy to EC2') {
            steps {
                echo 'Deploying...'
            }
        }
    }
}
